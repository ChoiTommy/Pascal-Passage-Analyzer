program pos_test;
uses longStringType, crt;
const s = 'Hi there this is here.';
var a : longString;
    x : IntegerArray;
    i : Integer;
begin
    {SetLength(a, Length(s));
    for i := 0 to Length(s)-1 do
        a[i] := s[i+1];

    x := posOfString('here', a);
    for i := 0 to Length(x)-1 do
        WriteLn(x[i]);}

    WriteLn('Abcd jsfklasn kljfa jkjfkajlsdff');
    Write('jfgklsjixjf ');
    TextBackground(LightGray);
    Write('shjkxjejhxkje');
    TextBackground(Black);
    Write(' jkkjxije skjklsj.');
    ReadLn;
    GotoXY(1, 1);
    DelLine;
    ReadLn;
end.